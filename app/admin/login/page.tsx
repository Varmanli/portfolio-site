import LoginForm from "@/components/admin/LoginForm";

/**
 * Login Page
 *
 * The admin login page that renders the login form.
 *
 * @returns {JSX.Element} The login page component
 */
export default function LoginPage() {
  return <LoginForm />;
}
